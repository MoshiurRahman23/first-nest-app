'use client';
import React from 'react'

const AddToCard = () => {
    return (
        <div>
            <button className='btn btn-primary' onClick={() => console.log("Click Me")}>Add to Card</button>
        </div>
    )
}

export default AddToCard